# 로컬 개발 (SQLite 파일)
DATABASE_URL="file:./dev.db"
# DATABASE_AUTH_TOKEN은 로컬에서는 불필요 (Turso 전용)

# --- 프로덕션(Turso) 예시 ---
# DATABASE_URL="libsql://your-db.turso.io"
# DATABASE_AUTH_TOKEN="your-turso-auth-token"
